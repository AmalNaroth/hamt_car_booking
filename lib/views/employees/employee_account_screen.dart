import 'package:flutter/material.dart';

class EmployeesAccountScreen extends StatelessWidget {
  const EmployeesAccountScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}